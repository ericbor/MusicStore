<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Annotation handlers (Applied by default to ALL @controllers -->
    <mvc:annotation-driven />
    <!-- Scanning enablement on package -->
    <context:component-scan base-package="music.controllers" />
    <mvc:resources location="/resources/" mapping="/resources/**" />

    <!-- Views mapped in JSPs under /WEB-INF/views -->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <bean id="songDao" class="music.dao.impl.SongDaoImpl">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <bean id="productDao" class="music.dao.impl.ProductDaoImpl">
        <property name="dataSource" ref="dataSource"/>
    </bean>


    <bean name="catalogController" class="music.controllers.CatalogController">
        <property name="productDao" ref="productDao" />
    </bean>

    <bean name="adminController" class="music.controllers.AdminController" />
    <bean name="orderController" class="music.controllers.OrderController" />
    <bean name="userController" class="music.controllers.UserController" />

    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${jdbc.driverClassName}" />
        <property name="url" value="${jdbc.url}" />
        <property name="username" value="${jdbc.username}" />
        <property name="password" value="${jdbc.password}" />
    </bean>

    <context:property-placeholder location="jdbc.properties"/>

    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource"></property>
    </bean>

</beans>